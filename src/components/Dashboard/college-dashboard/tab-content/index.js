import React from 'react';
// import styled from 'styled-components';

//  ************ Members Tab Style ************



const Dashboard = props => (
    <React.Fragment>
        Dashboard
    </React.Fragment>
)

const Events = props => (
    <React.Fragment>
        Events
    </React.Fragment>
)

const Articles = props => (
    <React.Fragment>
        Articles
    </React.Fragment>
)

const Billing = props => (
    <React.Fragment>
        Billing
    </React.Fragment>
)

const Settings = props => (
    <React.Fragment>
        Settings
    </React.Fragment>
)

export { Dashboard, Events, Articles, Billing, Settings }